// src/app/[locale]/partners/page.js

import Partners from '../../../pageComponents/Partners';

// This is the component Next.js executes when a user visits /partners
export default function PartnersRoute() {
  return (
    <Partners />
  );
}